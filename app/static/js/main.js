/**
 * main.js – client-side entry point
 *
 * Keep this file light.  Heavy UI logic should be split into separate
 * ES modules and imported here as the project grows.
 */

document.addEventListener("DOMContentLoaded", () => {
  console.log("Portfolio app ready.");
});
